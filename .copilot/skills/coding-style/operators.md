# Operators

## Applies
Applies to operator usage.

## Rules

minimize strict comparison
: only apply strict equality `===` and `!==` when required, otherwise rely on the type system
prefer `? :` of `if`-`else`
: prefer tertiary operator `? :` over if else trees as soon as assignment is done to the same variable

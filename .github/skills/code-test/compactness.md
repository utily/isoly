# Compactness

## Applies
Applies to the test file (`*.spec.ts`) as a whole.

## Rules
no blank lines between tests
: Don't add any blank lines between tests
single line tests
: Prefer single line tests and don't use braces for lambda functions with only one line

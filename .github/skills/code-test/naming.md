# Naming

## Applies
Naming of test and fixtures

## Rules
fixture for function
: Group tests by function tested in fixture with function name.
name test by arguments
: Use arguments to name test, preferably like this: `(argument) == expected`.
